# AGENTS.md — eNSP-MCP 项目最高优先级开发规则

> 本文件对项目内所有 AI 开发者（包括 Codex）具有最高约束力。
> 任何代码修改必须在遵循本文件规则的前提下进行。

---

## 核心原则

### 1. 小步修改原则

- 每次修改只做一件事，改动范围尽可能小。
- 单次提交修改的核心文件不超过 8-10 个。
- 多个不相关的改动拆分为独立提交。
- 优先使用 `apply_patch` 进行精准编辑，避免全文重写。

### 2. 保持兼容原则

- **禁止删除任何公开接口**（MCP tool、Flask endpoint、公共方法）。
- **禁止修改现有接口的签名**（参数名、类型、返回值结构）。
- 新增功能不得破坏已有功能的正确性。
- 配置项变更必须有默认值，确保不传参数时行为不变。

### 3. 可验证原则

- 每次修改后必须运行 `pytest tests/`，确保所有测试通过。
- 新增功能必须同步新增测试。
- 修改关键模块（mcp_server/knowledge/command_executor）时必须运行全量测试。
- 任何优化都必须保持现有功能正确。

### 4. 禁止大规模重写

- 禁止一次性重写整个模块或文件。
- 禁止"推倒重来"式的架构变更。
- 必须采用**渐进式重构**：每次只改一个关注点，验证后再改下一个。
- 重构前必须先建立安全网（创建分支、跑测试、建立基线）。

### 5. 修改前先阅读 docs

- 修改任何模块前，必须先阅读 `docs/modules/` 下对应的模块文档。
- 修改架构级行为前，必须先阅读 `docs/architecture/`。
- 修改完成后必须同步更新相关文档。

### 6. 完成后同步更新文档

- 新增/修改接口 → 更新 `docs/modules/` 对应文档。
- 架构决策变更 → 新增 `docs/adr/` 条目。
- 新增最佳实践 → 在 `docs/modules/` 对应模块文档中补充。

---

## 开发流程

```
1. 阅读 AGENTS.md（本文件）
2. 阅读 docs/ 相关文档
3. 创建 codex/ 前缀分支
4. 运行现有测试确认基线
5. 小步修改（每步 ≤10 个文件）
6. 每步运行测试
7. 提交（含变更说明）
8. 更新文档
```

---

## 禁止事项

- ❌ 直接修改 main/master 分支
- ❌ 删除或重命名公开 API
- ❌ 跳过测试直接提交
- ❌ 一次修改超过 10 个核心文件不拆分
- ❌ 修改业务逻辑时不先读文档
- ❌ 重构时推倒重来

---

## 项目结构速览

```
eNSP-MCP/
├── mcpensp1/               # 主项目代码
│   ├── mcp_server.py       # MCP 服务器（47 工具）
│   ├── app.py              # Flask Web 应用
│   ├── device_manager.py   # 设备连接池管理
│   ├── command_executor.py # 命令执行引擎
│   ├── connection.py       # Telnet 连接层
│   ├── heartbeat.py        # 心跳监控 + 设备保活
│   ├── view_router.py      # 命令视图感知路由 + VRP冷却保护
│   ├── knowledge.py        # 知识库核心（简化版：只记配置方法）
│   ├── topology.py         # 拓扑引擎
│   ├── services.py         # 服务层
│   ├── agent/              # AI Agent 子系统
│   │   ├── planner.py      # DAG 任务规划
│   │   ├── runtime.py      # 闭环执行引擎
│   │   ├── verifier.py     # 语义验证器
│   │   ├── reflection.py   # 反思引擎
│   │   ├── learning.py     # 学习引擎
│   │   ├── memory.py       # 长期记忆
│   │   └── knowledge_store.py # 增长知识库
│   └── kb/                 # 静态知识库数据
├── tests/                  # 测试（223 passed, 3 skipped）
└── docs/                   # 项目文档（本知识库）
    ├── architecture/       # 架构文档
    ├── development/        # 开发规范
    ├── modules/            # 模块文档
    └── adr/                # 架构决策记录
```
