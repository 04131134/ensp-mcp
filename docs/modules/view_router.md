# ViewRouter ? ????????

## ??
`mcpensp1/view_router.py`?v2.4 ???

## ??
????????????????? VRP ??????? `< >` / ???? `[ ]`??
???????????????????? VRP ?? `quit` ?? 3 ?????

## ????

### 1. ????
- `classify(cmd)` ? ??????????
  - `display *`, `ping`, `tracert`, `save`, `system-view` ? ????
  - `interface *`, `vlan`, `ospf`, `dhcp`, `firewall`, `undo *` ? ? ????
  - `quit`, `return` ? ????

### 2. ????
- `detect_view(conn)` ? ??????? `current_view` ??

### 3. ????
- `ensure_view(conn, path, required_view)` ? ??????????
  - ??????????? ? ?? `system-view`
  - ??????????? ? ?? `return`

### 4. VRP ????
- ????????? `quit`/`return` ??
- ?? ?3 ??????? 3 ?????VRP ????
- ? `KEEPALIVE_INTERVAL` ????????? 120 ??

### 5. ????
- `check_command_error(output)` ? ???????? VRP ?????
  `Error:`, `Unrecognized command`, `Wrong parameter`,
  `Too many parameters`, `Ambiguous command`, `Incomplete command`

## ????

```python
from view_router import view_router

# ???
view_router.before_command(conn, path, "interface GigabitEthernet0/0/1")
# ?????????

# ???
error = check_command_error(output)
if not error["success"]:
    print(f"Command failed: {error['errors']}")
```

## ????
```
app.py/send_command ? view_router (????? _needs_system/_needs_user ????)
app.py/send_command_batch ? view_router.before_command()
app.py ? check_command_error()
mcp_server.py/_direct_batch_cmd ? ???? error check
```

## ?????????

| ?? | ??? | ?? |
|------|--------|------|
| `KEEPALIVE_INTERVAL` | `120` | ?????????? 0 ?? |
